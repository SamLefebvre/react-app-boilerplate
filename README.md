# Boilerplate for React

## Install

Install dependencies:
`yarn install`

### Run

Webpack:
`yarn run dev-server`

### Test
`yarn test --watch`

### Code formatting

Check:
`yarn format:check`

Formatting all files:
`yarn format:write`
